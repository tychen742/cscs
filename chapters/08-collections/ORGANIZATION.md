---
orphan: true
---

# Chapter Organization — Data Collections

## Learning Objectives

Students should be able to choose between arrays, lists, and dictionaries for
common storage tasks; write basic `List<T>` code for growing ordered data; and
write basic `Dictionary<TKey,TValue>` code for key/value lookup.

## Sequence

- Data collections (conceptual foundations: access patterns, generics, namespaces)
- Lists (`List<T>`)
- Dictionaries (`Dictionary<TKey,TValue>`)

## Notes

- Review `0802-list-dictionary.ipynb` and merge any useful content into the active List and Dictionary notebooks before removing or archiving it.
- The reusable collection-family overview from `1101-intro-ds.ipynb` has been migrated into `0801-collections.ipynb`.
- The reusable `List<T>` basics from `1102-collection-examples.ipynb` have been migrated into `0802-list.ipynb`.
- `HashSet<T>` stays out of Chapter 8 by design — it is taught in Chapter 17 alongside hash tables, not as a loose "applied extension."
- Keep stacks and queues out of the active Chapter 8 path; introduce them with linear data structures later.
