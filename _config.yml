# Book settings
title: Introduction to Computer Science in C#
author: tychen
logo: images/logo.svg

# Force re-execution of notebooks on each build
execute:
  execute_notebooks: "off"   # set to "auto" to run C# notebooks on build

# MyST substitutions (available in .ipynb and .md files)
parse:
  myst_enable_extensions:
    - colon_fence
    - substitution
  myst_substitutions:
    if_else: "`if`-`else`"
    if_else_if: "`if`-`else`-`if`"
    do_while: "`do`-`while`"
    vscode: "Visual Studio Code"
    vscode_toggle: "Toggle Panel"
    vscode_terminal: "TERMINAL"
    repsrc: "https://github.com/tychen742/cscs/tree/main/examples"
    # repsrc: "examples"  # local copy of repo examples

# Only build files listed in _toc.yml
only_build_toc_files: true

# Exclude virtualenv and other non-book content
exclude_patterns:
  - .venv/**
  - _build/**
  - "**/.DS_Store"
  - README.md
  - cscs/

# HTML-specific settings
html:
  use_issues_button: false
  use_repository_button: false

# Sphinx settings (carried over from your existing conf.py)
sphinx:
  extra_extensions:
    - sphinx.ext.todo
    - sphinx.ext.mathjax
    - sphinxcontrib.youtube
  config:
    html_static_path:
      - _static
      - images
    html_favicon: _static/favicon.png
    html_css_files:
      - my-styles.css
      - custom.css
      - https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css
    html_js_files:
      - custom.js
    mathjax_path: https://cdn.jsdelivr.net/npm/mathjax@3/es5/tex-mml-chtml.js
    todo_include_todos: true
    numfig: true
    highlight_language: csharp
    highlight_options:
      default: { linenos: true }
      # csharp: { linenos: true }
    nb_number_source_lines: true
    rst_prolog: |
      .. role:: repsrc
    rst_epilog: |
      .. |if-else| replace:: ``if``\ -``else``

      .. |if-else-if| replace:: ``if``\ -``else``\ -``if``

      .. |do-while| replace:: ``do``\ -``while``

      .. |repsrc| replace:: https://github.com/tychen742/cscs/tree/main/examples
    thebe_config:
      requestKernel: true
      jupyterhubUrl: "https://thinkcscs.org"
      token: "11b92943ad141088b548a87952ea88ea7567c66406934fdc4032947b6fdeb80c"
      kernelOptions:
        name: ".net-csharp"
        path: "."
      binderOptions:
        repo: ""
        ref: ""
      predefinedOutput: true

thebe:
  kernel_name: .net-csharp
  jupyter_url: https://thinkcscs.org
  token: "11b92943ad141088b548a87952ea88ea7567c66406934fdc4032947b6fdeb80c"
  use_thebe_lite: false
  binder_url: ""

# Launch button settings (optional - for Binder/JupyterHub)
launch_buttons:
  thebe: true

# # Repository info (optional)
repository:
  url: https://github.com/tychen742/cscs
  branch: main
  path_to_book: .
