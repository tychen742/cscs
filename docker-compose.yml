services:
  csharp-kernel:
    build:
      context: .
      dockerfile: jupyter/Dockerfile
    ports:
      - "127.0.0.1:8888:8888"
    volumes:
      - .:/workspace
    environment:
      JUPYTER_TOKEN: ""
    restart: unless-stopped
